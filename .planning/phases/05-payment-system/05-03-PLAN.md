---
phase: 05-payment-system
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/paywall.ts
  - src/pages/premium-pricing.ts
  - src/pages/subscription-management.ts
autonomous: true

must_haves:
  truths:
    - "Paywall page displays two plans: Try It Out ($30/mo) and Better Together ($240/yr)"
    - "Premium pricing page displays the same two plans with consistent pricing"
    - "Subscription management page redirects to Stripe Customer Portal for all management actions"
    - "No $39 or $69 prices appear anywhere in the codebase"
    - "Checkout form submits planId (not tierId) matching STRIPE_PLANS keys"
  artifacts:
    - path: "src/pages/paywall.ts"
      provides: "Updated paywall with two-plan pricing ($30/mo, $240/yr)"
      contains: "$30"
    - path: "src/pages/premium-pricing.ts"
      provides: "Updated premium pricing matching paywall"
      contains: "$30"
    - path: "src/pages/subscription-management.ts"
      provides: "Management page using Stripe Customer Portal"
      contains: "create-portal-session"
  key_links:
    - from: "src/pages/paywall.ts"
      to: "/api/payments/create-checkout-session"
      via: "fetch POST with planId"
      pattern: "planId.*try-it-out|planId.*better-together"
    - from: "src/pages/subscription-management.ts"
      to: "/api/payments/create-portal-session"
      via: "fetch POST for portal redirect"
      pattern: "create-portal-session"
---

<objective>
Unify pricing across all pages to match STRIPE_PLANS (2 plans: $30/mo, $240/yr) and wire subscription management to Stripe Customer Portal.

Purpose: PAY-06 (single consistent pricing), PAY-04 (cancellation works via portal). Eliminates the conflicting $39/$69 pricing from paywall.ts and aligns all pages with the Stripe Products.

Output: All three payment-related pages show consistent pricing and use the new API endpoints.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-payment-system/05-RESEARCH.md
@.planning/phases/05-payment-system/05-01-SUMMARY.md
@src/pages/paywall.ts
@src/pages/premium-pricing.ts
@src/pages/subscription-management.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update paywall.ts with consolidated two-plan pricing</name>
  <files>src/pages/paywall.ts</files>
  <action>
    Rewrite the paywall page pricing to show exactly TWO plans matching STRIPE_PLANS:

    **Plan 1: Try It Out**
    - Price: $30/month
    - Plan ID sent to API: `'try-it-out'`

    **Plan 2: Better Together (Annual)**
    - Price: $240/year ($20/month)
    - Plan ID sent to API: `'better-together'`
    - Mark as "BEST VALUE" or "SAVE 33%"

    **Changes to the HTML template:**

    1. **Feature comparison table header**: Change "$39" to "$30/mo" and "$69" to "$240/yr ($20/mo)". Rename "Growing Together" to "Try It Out" and "Growing Together+" to "Better Together".

    2. **Plan selection tabs**: Rename to "Monthly" and "Annual" (or "Try It Out" and "Better Together").

    3. **Plan selection radio buttons**: Update values from 'basic'/'premium' to 'monthly'/'annual'. Update displayed prices to $30 and $240.

    4. **Feature list**: Simplify to match the actual freemium model:
       - Try It Out: AI Coach (basic), daily check-ins, relationship challenges, community access
       - Better Together: Everything in Try It Out + advanced AI coach, video calls, intimacy challenges, priority support, all assessments

    5. **Remove fake social proof section**: The "Join 50,000+ Happy Couples" section with 87%/94%/98% stats and fake testimonial ("Sarah & Mike, married 8 years") -- remove entirely. This is fake data (INT-02 territory but we should not add MORE fake data while rebuilding).

    6. **Remove "Limited Time Offer"**: The 50% off first month claim is fake.

    7. **Remove "Monthly Surprise Box"** from features table -- this is a v2/deferred feature (PROD-01).

    8. **JavaScript form submission**: Update the `tierIdMap` to:
       ```javascript
       const planIdMap = {
         'monthly': 'try-it-out',
         'annual': 'better-together'
       };
       ```
       Change the fetch body from `tierId` to `planId`.

    9. **Price display**: When monthly selected show "$30/month", when annual selected show "$240/year ($20/month)".

    Keep the overall visual design (gradient background, card layout, Stripe checkout redirect flow). Just fix the content/pricing.
  </action>
  <verify>
    - `grep "\\$39\|\\$69" src/pages/paywall.ts` returns NO results (old prices removed)
    - `grep "\\$30" src/pages/paywall.ts` returns results (new monthly price)
    - `grep "\\$240" src/pages/paywall.ts` returns results (new annual price)
    - `grep "planId" src/pages/paywall.ts` returns results (new field name)
    - `grep "50,000" src/pages/paywall.ts` returns NO results (fake social proof removed)
    - `grep "tierId" src/pages/paywall.ts` returns NO results (old field name gone)
  </verify>
  <done>Paywall shows two plans ($30/mo Try It Out, $240/yr Better Together), submits planId to API, no fake social proof.</done>
</task>

<task type="auto">
  <name>Task 2: Update premium-pricing.ts and subscription-management.ts</name>
  <files>src/pages/premium-pricing.ts, src/pages/subscription-management.ts</files>
  <action>
    **premium-pricing.ts:**
    Update to show the same two plans as paywall. This page currently has an "Annual-First Strategy" with different pricing. Align it:
    - Try It Out: $30/month
    - Better Together: $240/year ($20/month)
    - Keep the annual-first visual emphasis (that's fine), just fix the dollar amounts
    - Update any checkout buttons to send `planId` (not `tierId`) to `/api/payments/create-checkout-session`
    - Remove "No Free Tier Available" messaging -- we ARE implementing a free tier (PAY-02)
    - Add a note that free users get basic features, premium unlocks everything

    **subscription-management.ts:**
    Simplify by leveraging Stripe Customer Portal:

    1. **"Open Billing Portal" button**: Keep this -- it calls `/api/payments/create-portal-session`
    2. **"Change Plan" button**: Instead of navigating to `/premium-pricing`, redirect to portal (plan changes handled by Stripe)
    3. **"Pause Subscription" button**: Remove -- Stripe doesn't support pause out of the box for basic integration. Or change to navigate to portal.
    4. **"Cancel Subscription" button**: Keep the modal UX, but on confirm, call `/api/payments/cancel-subscription` (which uses SDK) and then suggest they can manage via portal too
    5. **"Update Payment Method" button**: Redirect to portal (Stripe handles this)
    6. **Billing history section**: Keep the fetch to `/api/payments/billing-history` (this endpoint doesn't exist yet -- that's OK, the page gracefully shows "No billing history" on error)
    7. **Plan names**: Update from "Monthly Premium"/"6-Month Premium"/"Annual Premium" to match STRIPE_PLANS names ("Try It Out", "Better Together")
    8. **Update the API call**: Change `userId: localStorage.getItem('userId')` pattern -- this still works since auth middleware provides userId
  </action>
  <verify>
    - `grep "\\$39\|\\$69" src/pages/premium-pricing.ts` returns NO results
    - `grep "\\$30" src/pages/premium-pricing.ts` returns results
    - `grep "\\$240" src/pages/premium-pricing.ts` returns results
    - `grep "No Free Tier" src/pages/premium-pricing.ts` returns NO results
    - `grep "create-portal-session" src/pages/subscription-management.ts` returns results
    - `grep "Try It Out\|Better Together" src/pages/subscription-management.ts` returns results
    - `npm run build` succeeds
  </verify>
  <done>All three payment pages show consistent pricing ($30/mo, $240/yr). Subscription management uses Stripe Customer Portal. No conflicting prices remain.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `grep -r "\\$39\|\\$69" src/pages/` returns NO results (old pricing eliminated)
- All payment pages reference $30 and $240 prices
- `grep -r "tierId" src/pages/` returns NO results (using planId everywhere)
- Subscription management page uses portal session endpoint
</verification>

<success_criteria>
1. Paywall shows exactly two plans: Try It Out ($30/mo) and Better Together ($240/yr)
2. Premium pricing page matches paywall pricing
3. Subscription management uses Stripe Customer Portal for plan changes and payment updates
4. No fake social proof (50,000+ couples, 87% stats) appears
5. No conflicting prices ($39, $69) remain in any page
6. Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-payment-system/05-03-SUMMARY.md`
</output>
