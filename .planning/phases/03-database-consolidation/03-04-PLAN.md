---
phase: 03-database-consolidation
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/api/challenges.ts
  - src/api/relationships.ts
  - src/api/posts.ts
  - src/api/feed.ts
  - src/api/discovery.ts
  - src/api/analytics.ts
  - src/api/messaging.ts
  - src/api/communities.ts
  - src/api/social.ts
  - supabase-migrations/006_search_functions.sql
autonomous: true

must_haves:
  truths:
    - "All 9 Tier 3+4 API files use Supabase client instead of Neon"
    - "No import of createDatabase or ../db in any of these files"
    - "discovery.ts mixed placeholder bugs eliminated"
    - "Complex queries use Supabase RPC where query builder is insufficient"
    - "All endpoints validate input with Zod schemas"
  artifacts:
    - path: "src/api/discovery.ts"
      provides: "Discovery search via Supabase query builder + RPC"
      contains: "createAdminClient"
    - path: "src/api/social.ts"
      provides: "Social interactions via Supabase (largest file)"
      contains: "createAdminClient"
    - path: "src/api/messaging.ts"
      provides: "Messaging via Supabase"
      contains: "createAdminClient"
    - path: "src/api/communities.ts"
      provides: "Communities via Supabase"
      contains: "createAdminClient"
    - path: "supabase-migrations/006_search_functions.sql"
      provides: "PostgreSQL RPC functions for complex discovery queries"
      contains: "CREATE OR REPLACE FUNCTION"
  key_links:
    - from: "src/api/discovery.ts"
      to: "supabase-migrations/006_search_functions.sql"
      via: "supabase.rpc() calls"
      pattern: "supabase\\.rpc\\("
    - from: "src/api/social.ts"
      to: "src/lib/supabase/server.ts"
      via: "import createAdminClient"
      pattern: "import.*createAdminClient"
---

<objective>
Migrate 9 complex API files (Tier 3+4) from Neon raw SQL to Supabase, including the hardest files: discovery.ts (dynamic SQL), social.ts (1044 lines, 56 calls), messaging.ts (44 calls), and communities.ts (45 calls). Create RPC functions for queries too complex for the query builder.

Purpose: These are the most complex migrations with dynamic SQL, conditional joins, and many DB calls. Completing them eliminates all Neon usage from the codebase.
Output: 9 API files fully on Supabase, 1 new migration file with RPC functions, zero Neon imports anywhere.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-database-consolidation/03-RESEARCH.md
@.planning/phases/03-database-consolidation/03-01-SUMMARY.md
@src/lib/supabase/server.ts
@src/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate challenges.ts, relationships.ts, posts.ts, feed.ts (Tier 3)</name>
  <files>src/api/challenges.ts, src/api/relationships.ts, src/api/posts.ts, src/api/feed.ts</files>
  <action>
These files have 17-28 DB calls each. Apply the standard migration pattern but pay extra attention to:

**challenges.ts** (309 lines, 17 calls): Has challenge progress tracking with multiple related queries. Convert JOINs to Supabase nested selects where possible: `supabase.from('challenges').select('*, challenge_participants(*)')`. For queries that join multiple tables, break into sequential calls if the query builder cannot express the join.

**relationships.ts** (438 lines, 18 calls): Core relationship CRUD plus partner invitations. Has queries checking mutual relationship membership. Convert to chained `.eq()` and `.or()` filters.

**posts.ts** (412 lines, 20 calls): Social posts with likes, comments, media. Use Supabase nested selects for post+author+likes in one query: `.from('posts').select('*, users!inner(name, profile_photo_url), post_likes(count)')`.

**feed.ts** (476 lines, 28 calls): Complex feed assembly. This file likely builds feeds from multiple sources. Convert each query individually. If feed assembly uses complex UNION queries, consider breaking into parallel Supabase queries and merging in JavaScript.

Standard rules apply:
- Use `createAdminClient` (service role, bypasses RLS)
- Add Zod validation to all endpoints accepting input
- Keep response shapes identical
- Use `.maybeSingle()` for optional lookups
- Handle PGRST116 errors from `.single()`
  </action>
  <verify>`grep -r "createDatabase\|from.*'../db'" src/api/challenges.ts src/api/relationships.ts src/api/posts.ts src/api/feed.ts` returns no results. All 4 files contain `createAdminClient`.</verify>
  <done>challenges.ts, relationships.ts, posts.ts, feed.ts all use Supabase query builder with Zod validation. Zero Neon imports.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate discovery.ts, analytics.ts, messaging.ts, communities.ts, social.ts (Tier 4)</name>
  <files>src/api/discovery.ts, src/api/analytics.ts, src/api/messaging.ts, src/api/communities.ts, src/api/social.ts, supabase-migrations/006_search_functions.sql</files>
  <action>
These are the hardest files. Each has 37-56 DB calls and some have complex dynamic SQL.

**discovery.ts** (936 lines, 37 calls):
- This file has MIXED PLACEHOLDER BUGS (`$N` and `?` styles). Do NOT try to fix the SQL -- replace it entirely.
- For simple queries (trending, recent, featured): Use Supabase query builder
- For complex search with dynamic joins, blocked-user filtering, and conditional ordering: Create PostgreSQL functions in `supabase-migrations/006_search_functions.sql` and call via `supabase.rpc()`. See research doc Example 3 for the pattern.
- Create at minimum: `search_users_v1(p_query, p_user_id, p_limit, p_offset)`, `search_communities_v1(...)`, `search_posts_v1(...)`
- The `blockedIds.map(() => '?').join(', ')` pattern MUST be eliminated -- RPC functions handle blocked filtering internally.

**analytics.ts** (973 lines, 38 calls): Heavy aggregation queries. Convert simple counts to `.select('*', { count: 'exact', head: true })`. For complex aggregations (GROUP BY, date ranges, computed metrics), create RPC functions in the same 006 migration file.

**messaging.ts** (839 lines, 44 calls): Conversation listing, message sending, read receipts. Convert to query builder. For the N+1 conversation listing query (identified in requirements PERF-01), use a single query with nested selects or defer the optimization to Phase 7.

**communities.ts** (859 lines, 45 calls): Community CRUD, membership, posts, moderation. Straightforward but many calls. Convert methodically, one endpoint at a time.

**social.ts** (1044 lines, 56 calls): Largest file. Follow/unfollow, block, report, activity feed, connections. Convert all 56 calls to query builder. This is tedious but mechanical -- each call follows the same pattern.

Standard rules apply:
- Use `createAdminClient` for ALL routes
- Add Zod validation to all endpoints
- Keep response shapes identical
- Use `.maybeSingle()` for optional lookups

WARNING: This task touches 5 large files (4400+ lines total). The executor should work through them methodically, one file at a time, verifying each compiles before moving to the next.
  </action>
  <verify>`grep -r "createDatabase\|from.*'../db'" src/api/discovery.ts src/api/analytics.ts src/api/messaging.ts src/api/communities.ts src/api/social.ts` returns no results. `grep "rpc(" src/api/discovery.ts` shows RPC calls for search functions. `ls supabase-migrations/006_search_functions.sql` exists.</verify>
  <done>discovery.ts, analytics.ts, messaging.ts, communities.ts, social.ts all use Supabase query builder + RPC. Zero Neon imports. Mixed placeholder bugs in discovery.ts eliminated. 006_search_functions.sql created with RPC functions for complex queries.</done>
</task>

</tasks>

<verification>
- `grep -r "createDatabase\|from.*'../db'" src/api/` returns NO results across the entire api/ directory
- `grep -r "blockedIds.map.*join" src/api/discovery.ts` returns no results (placeholder bug gone)
- `ls supabase-migrations/006_search_functions.sql` exists
- `npx tsc --noEmit` compiles without errors
</verification>

<success_criteria>
- All 9 Tier 3+4 files fully migrated from Neon to Supabase
- discovery.ts placeholder bugs eliminated, search uses RPC functions
- All endpoints have Zod input validation
- Zero createDatabase imports anywhere in src/api/
- DB-04 partially satisfied: No raw SQL string interpolation in migrated files
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-consolidation/03-04-SUMMARY.md`
</output>
