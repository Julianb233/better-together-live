---
phase: 03-database-consolidation
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - src/db.ts
  - src/db-supabase.ts
  - package.json
  - src/index.tsx
autonomous: true

must_haves:
  truths:
    - "src/db.ts (Neon adapter) is deleted"
    - "@neondatabase/serverless removed from package.json"
    - "No file in src/ imports from db.ts"
    - "TypeScript compiles with zero errors"
    - "Build succeeds (npm run build)"
  artifacts:
    - path: "package.json"
      provides: "Dependencies without @neondatabase/serverless"
    - path: "src/db-supabase.ts"
      provides: "Cleaned Supabase wrapper (raw .query() method removed)"
  key_links:
    - from: "src/api/*.ts"
      to: "src/lib/supabase/server.ts"
      via: "all API files import from supabase"
      pattern: "import.*from.*lib/supabase"
---

<objective>
Delete the Neon database adapter, remove @neondatabase/serverless from dependencies, clean up db-supabase.ts, verify the entire project builds cleanly with Supabase as the sole database.

Purpose: Final cleanup to achieve DB-01 (all routes use Supabase), DB-02 (single migration system), and DB-05 (Supabase client is sole DB connection).
Output: Clean codebase with zero Neon references, passing build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-database-consolidation/03-RESEARCH.md
@.planning/phases/03-database-consolidation/03-02-SUMMARY.md
@.planning/phases/03-database-consolidation/03-03-SUMMARY.md
@.planning/phases/03-database-consolidation/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Neon adapter, remove dependency, clean up imports</name>
  <files>src/db.ts, package.json, src/index.tsx, src/db-supabase.ts</files>
  <action>
1. **Verify no remaining Neon imports**: Run `grep -r "createDatabase\|from.*['\"]\.\.\/db['\"]]\|from.*['\"]\.\/db['\"]" src/` and confirm zero results. If any remain, they are bugs from plans 02-04 that must be fixed first.

2. **Delete src/db.ts**: `rm src/db.ts`

3. **Remove @neondatabase/serverless**: `npm uninstall @neondatabase/serverless`

4. **Clean src/db-supabase.ts**: Read the file. Remove the `.query()` method that calls the nonexistent `rpc('execute_sql')` (per research: "never use this method"). Keep any useful wrapper methods that use the query builder. If the entire file is just the broken query method + redundant wrappers around createAdminClient, delete the file entirely and update any imports.

5. **Check src/index.tsx**: Verify it does not import from db.ts. If it does, remove the import. Check if DATABASE_URL is referenced in env type definitions -- if so, remove it (no longer needed).

6. **Check src/types.ts**: If it defines a DATABASE_URL in the Env type, remove it. Keep SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY.

7. **Remove old D1 migration files**: The `migrations/` directory contains old D1/SQLite migration files (0001-0005). These are superseded by `supabase-migrations/`. Delete the entire `migrations/` directory and `scripts/migrate.js` if it only handles D1 migrations.

8. **Run full verification**:
   - `npx tsc --noEmit` -- must pass with zero errors
   - `npm run build` -- must succeed
   - `grep -r "neon\|@neondatabase" src/ package.json` -- must return nothing (except possibly lock file)
  </action>
  <verify>
`ls src/db.ts 2>/dev/null` returns "No such file". `grep "@neondatabase" package.json` returns nothing. `npx tsc --noEmit` exits 0. `npm run build` exits 0.
  </verify>
  <done>src/db.ts deleted. @neondatabase/serverless removed from package.json. db-supabase.ts cleaned (broken .query() removed). TypeScript compiles. Build succeeds. Zero Neon references in codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Verify RLS policies and final audit</name>
  <files>supabase-migrations/002_row_level_security.sql</files>
  <action>
1. **Audit RLS migration**: Read `supabase-migrations/002_row_level_security.sql`. Check that column names in RLS policies match the actual table column names used by the API routes. The research flagged this as a potential issue (DB-03: "column names match").

2. **Check for common RLS issues**:
   - Policies referencing `auth.uid()` -- this returns the Supabase Auth user ID. Verify our users table `id` column matches what `auth.uid()` returns.
   - Policies on tables that don't exist yet (tables created in later migrations)
   - Missing policies for tables added in migrations 003-005

3. **Do NOT enable RLS enforcement now**: Per research recommendation, we use `createAdminClient` (service role, bypasses RLS) for all current routes. RLS enforcement is a separate future concern. Just verify the policies are syntactically correct and reference valid columns.

4. **Final grep audit**: Run these to confirm complete migration:
   - `grep -r "createDatabase" src/` -- must return nothing
   - `grep -r "from.*db'" src/api/` -- must return nothing (except db-supabase if kept)
   - `grep -r "\.all<any>\|\.first<any>\|\.run(" src/api/` -- must return nothing (old Neon patterns)
   - `grep -r "zValidator\|zodErrorHandler" src/api/ | wc -l` -- should be substantial (validation in use)
  </action>
  <verify>All 4 grep audits pass. `npm run build` still succeeds after any RLS fixes.</verify>
  <done>RLS policies audited for column name correctness. Complete migration verified via grep audit: zero Neon patterns, Zod validation present across API routes. Build passes.</done>
</task>

</tasks>

<verification>
- `ls src/db.ts 2>/dev/null` -- file does not exist
- `grep "@neondatabase" package.json` -- no results
- `grep -r "createDatabase" src/` -- no results
- `grep -r "\.all<any>\|\.first<any>" src/api/` -- no results
- `npx tsc --noEmit` -- exits 0
- `npm run build` -- exits 0
- `grep -c "zValidator" src/api/*.ts | grep -v ":0"` -- shows validation across many files
</verification>

<success_criteria>
- DB-01: All API routes use Supabase client (Neon adapter deleted)
- DB-02: supabase-migrations/ is sole migration source (old migrations/ deleted)
- DB-03: RLS policies audited for column correctness
- DB-04: No raw SQL string interpolation
- DB-05: Supabase client is sole database connection
- VAL-01: All API endpoints validate input with Zod
- VAL-02: Zod in package.json
- VAL-03: Invalid input returns 400 with descriptive errors
- Build passes, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-consolidation/03-05-SUMMARY.md`
</output>
