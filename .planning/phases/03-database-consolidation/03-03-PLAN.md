---
phase: 03-database-consolidation
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/api/users.ts
  - src/api/notifications.ts
  - src/api/recommendations.ts
  - src/api/payments.ts
  - src/api/sponsors.ts
  - src/api/push-notifications.ts
  - src/api/analytics-enhanced.ts
  - src/api/quiz.ts
autonomous: true

must_haves:
  truths:
    - "All 8 Tier 2 API files use Supabase client instead of Neon"
    - "No import of createDatabase or ../db in any Tier 2 file"
    - "All endpoints validate input with Zod schemas"
    - "Payment endpoints maintain exact same response shapes"
  artifacts:
    - path: "src/api/users.ts"
      provides: "User CRUD via Supabase"
      contains: "createAdminClient"
    - path: "src/api/payments.ts"
      provides: "Payment routes via Supabase"
      contains: "createAdminClient"
    - path: "src/api/push-notifications.ts"
      provides: "Push notification routes via Supabase"
      contains: "createAdminClient"
  key_links:
    - from: "src/api/users.ts"
      to: "src/lib/supabase/server.ts"
      via: "import createAdminClient"
      pattern: "import.*createAdminClient.*from.*lib/supabase"
    - from: "src/api/payments.ts"
      to: "src/lib/supabase/server.ts"
      via: "import createAdminClient"
      pattern: "import.*createAdminClient.*from.*lib/supabase"
---

<objective>
Migrate 8 Tier 2 (moderate complexity) API files from Neon raw SQL to Supabase query builder, adding Zod validation to all endpoints.

Purpose: These files have 6-10 DB calls each but are still straightforward conversions. They can run in parallel with Plan 02 since they touch different files.
Output: 8 API files fully using Supabase client + Zod validation, zero Neon imports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-database-consolidation/03-RESEARCH.md
@.planning/phases/03-database-consolidation/03-01-SUMMARY.md
@src/lib/supabase/server.ts
@src/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate users.ts, notifications.ts, recommendations.ts, quiz.ts</name>
  <files>src/api/users.ts, src/api/notifications.ts, src/api/recommendations.ts, src/api/quiz.ts</files>
  <action>
For each of the 4 files, apply the standard migration pattern:

1. Replace `import { createDatabase } from '../db'` with `import { createAdminClient, type SupabaseEnv } from '../lib/supabase'`
2. Replace `const db = createDatabase(c.env as Env)` with `const supabase = createAdminClient(c.env as SupabaseEnv)`
3. Convert all `db.all()`, `db.first()`, `db.run()` calls to Supabase query builder
4. Add Zod validation using schemas from `src/lib/validation/schemas/`
5. Use `.maybeSingle()` for lookups that may return no rows
6. Use `createAdminClient` (service role, bypasses RLS)
7. Keep response shapes identical

**users.ts specific** (269 lines, 7 calls): Has profile update with multiple optional fields. Use `.update()` with only the fields present in the validated body. Has profile photo URL handling -- keep as-is, just change DB layer.

**recommendations.ts specific** (197 lines, 6 calls): May have complex filtering. Convert WHERE clauses to chained `.eq()`, `.in()`, `.gte()` etc.

**quiz.ts specific** (205 lines): May use createDatabase even though research said "could be simple". Check and migrate if it does.
  </action>
  <verify>`grep -r "createDatabase\|from.*'../db'" src/api/users.ts src/api/notifications.ts src/api/recommendations.ts src/api/quiz.ts` returns no results.</verify>
  <done>users.ts, notifications.ts, recommendations.ts, quiz.ts all use Supabase query builder with Zod validation. Zero Neon imports.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate payments.ts, sponsors.ts, push-notifications.ts, analytics-enhanced.ts</name>
  <files>src/api/payments.ts, src/api/sponsors.ts, src/api/push-notifications.ts, src/api/analytics-enhanced.ts</files>
  <action>
Apply the standard migration pattern to these 4 files.

**payments.ts specific** (327 lines, 6 calls): This file handles Stripe webhooks and checkout. The Stripe logic stays unchanged -- only migrate the DB calls that store/retrieve subscription status. Be careful not to break the webhook signature verification added in Phase 1 (SEC-03).

**push-notifications.ts specific** (668 lines, 9 calls): Large file. It uses `jose` for APNS tokens (decision 02-03 kept jose). Only migrate the DB calls (device token storage, notification preferences). Keep APNS/FCM logic intact.

**sponsors.ts specific** (277 lines, 9 calls): Straightforward CRUD for sponsor data.

**analytics-enhanced.ts specific** (280 lines, 9 calls): Has some aggregate queries. Convert COUNT/SUM to Supabase `.select('*', { count: 'exact', head: true })` where possible, or use `.rpc()` for complex aggregations.

Same rules apply:
- Use `createAdminClient` (service role, bypasses RLS)
- Add Zod validation to all endpoints
- Keep response shapes identical
- Use `.maybeSingle()` for optional lookups
  </action>
  <verify>`grep -r "createDatabase\|from.*'../db'" src/api/payments.ts src/api/sponsors.ts src/api/push-notifications.ts src/api/analytics-enhanced.ts` returns no results.</verify>
  <done>payments.ts, sponsors.ts, push-notifications.ts, analytics-enhanced.ts all use Supabase query builder with Zod validation. Zero Neon imports.</done>
</task>

</tasks>

<verification>
- `grep -r "createDatabase\|from.*'../db'" src/api/users.ts src/api/notifications.ts src/api/recommendations.ts src/api/quiz.ts src/api/payments.ts src/api/sponsors.ts src/api/push-notifications.ts src/api/analytics-enhanced.ts` returns empty
- `grep -c "createAdminClient" src/api/users.ts src/api/payments.ts src/api/push-notifications.ts` shows counts for all 3
- `npx tsc --noEmit` compiles without errors
</verification>

<success_criteria>
- 8 Tier 2 files fully migrated from Neon to Supabase
- All 8 files have Zod input validation
- Zero createDatabase imports in these files
- Stripe webhook verification (Phase 1) still intact in payments.ts
- Push notification APNS/FCM logic intact in push-notifications.ts
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-consolidation/03-03-SUMMARY.md`
</output>
