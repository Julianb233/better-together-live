---
phase: 07-analytics-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/analytics.ts
autonomous: true

must_haves:
  truths:
    - "Analytics dashboard shows real data from database queries, never fake numbers"
    - "Error states return zeros with error flag, not realistic-looking fake data"
    - "No hardcoded user counts, revenue figures, or growth percentages in any response"
    - "Demographics and heatmap return empty/zero when no real data exists"
  artifacts:
    - path: "src/api/analytics.ts"
      provides: "All analytics endpoints with real-only data"
      contains: "error: 'Analytics temporarily unavailable'"
  key_links:
    - from: "src/api/analytics.ts"
      to: "supabase"
      via: "createAdminClient queries"
      pattern: "createAdminClient"
---

<objective>
Remove all mock/hardcoded analytics data from analytics.ts so every endpoint returns only real database-sourced data or explicit zeros with error flags.

Purpose: Satisfies ANL-01 (real data from database) and ANL-02 (real user counts and engagement metrics). Currently the file has extensive fake data: `isSupabaseAvailable` fallback blocks return numbers like 50247/25124/847, error catch blocks return fake data instead of zeros, hardcoded growth strings (+34.2%, +19.3%), fake demographics, mock generator functions, and hardcoded heatmap/system metrics.

Output: Clean analytics.ts where every response path returns real DB data or zeros + error flag.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-analytics-performance/07-RESEARCH.md
@src/api/analytics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all mock data fallbacks and fake numbers from analytics.ts</name>
  <files>src/api/analytics.ts</files>
  <action>
Systematically clean analytics.ts by applying these changes to every endpoint:

1. **Remove `isSupabaseAvailable` guard blocks** (lines 43-59, 148-176, 272-283, 358-373, 470-484, 567-576, 660-673): Delete the entire `if (!isSupabaseAvailable(c))` blocks that return fake data. The Supabase connection is now required infrastructure (Phase 3 migrated everything). If Supabase is truly down, the try/catch will handle it.

2. **Fix error catch blocks** to return zeros + error flag instead of fake numbers:
   - `/overview` catch (lines 128-144): Return `{ totalUsers: 0, engagedCouples: 0, partnerRevenue: 0, appSessions: 0, growthMetrics: { usersGrowth: 'N/A', couplesGrowth: 'N/A', revenueGrowth: 'N/A', sessionsGrowth: 'N/A' }, error: 'Analytics temporarily unavailable', lastUpdated: new Date().toISOString() }`
   - `/users` catch: Return zeros for all fields, empty arrays for demographics
   - `/revenue` catch: Return zeros, empty arrays
   - `/features` catch: Return zeros, empty arrays
   - `/partners` catch: Already correct (returns zeros) -- leave as-is
   - `/activity` catch: Already correct (returns empty array) -- leave as-is

3. **Remove hardcoded growth strings**: Line 123 `revenueGrowth: '+34.2%'` and line 124 `sessionsGrowth: '+19.3%'` -- calculate from real data or return 'N/A'. Add real revenue growth calculation by comparing current month vs previous month sponsor revenue. For sessions growth, return 'N/A' (no session tracking table exists).

4. **Remove hardcoded demographics** (lines 234-247): The users table likely has no age/location columns. Replace with empty arrays: `ageGroups: [], locations: []`. Keep the engagement section which already uses real DB queries.

5. **Remove hardcoded values in real data paths**:
   - Line 232: `growthData.length > 0 ? growthData : generateMockUserGrowth()` -- change to just `growthData`
   - Line 253: `averageSessionDuration: '12.5 minutes'` -- change to `averageSessionDuration: null`
   - Line 254: `sessionsPerUser: 4.8` -- change to `sessionsPerUser: null`
   - Line 330: `revenueData.length > 0 ? revenueData : generateMockRevenueData()` -- change to just `revenueData`
   - Line 341: `customerLifetimeValue: 420` -- change to `customerLifetimeValue: null`
   - Line 342: `repeatPurchaseRate: 68` -- change to `repeatPurchaseRate: null`
   - Lines 433-435: Feature usage fallback to fake array -- change to just `usageData`
   - Line 441: `averageTimeSpent: '8.5 minutes'` -- change to `averageTimeSpent: null`
   - Line 442: `topChallenges: ['Date Night Planning', ...]` -- query real top challenges from DB or return empty array
   - Line 540: `churnRate: 2.1` -- change to `churnRate: null`
   - Line 550: hardcoded metrics `bookingConversion: 12.4, repeatBookingRate: 34, customerSatisfaction: 89` -- change all to `null`

6. **Remove mock generator functions**: Delete `generateMockUserGrowth()` (lines 708-720), `generateMockRevenueData()` (lines 722-734), and `generateActivityHeatmap()` (lines 736-747).

7. **Fix `/system` endpoint** (lines 646-656): Replace hardcoded uptime/responseTime/errorRate/cacheHitRate/queryTime/requestsPerSecond/peakRPS/pageLoadTime/scores with a simple health check that reports whether Supabase is reachable:
   ```typescript
   analyticsApi.get('/system', async (c: Context) => {
     const supabaseAvailable = !!(c.env?.SUPABASE_URL && c.env?.SUPABASE_SERVICE_ROLE_KEY)
     return c.json({
       health: { status: supabaseAvailable ? 'healthy' : 'degraded', uptime: null, responseTime: null, errorRate: null },
       infrastructure: {
         database: { status: supabaseAvailable ? 'connected' : 'unavailable' },
         api: { status: 'healthy' }
       },
       performance: null
     })
   })
   ```

8. **Fix `/export` endpoint** (lines 658-705): Remove the `isSupabaseAvailable` guard block (lines 660-673) that returns fake export data with 50247 users.

9. **Remove heatmap references**: The `generateActivityHeatmap()` calls in `/features` endpoint (lines 371, 453, 463) return hardcoded data. Replace with `heatmap: null` since there's no real activity heatmap data source.

10. **Remove `isSupabaseAvailable` function** itself (lines 22-24) since it's no longer needed.
  </action>
  <verify>
    Run `grep -n "50247\|25124\|847\|Mock\|mock\|generateMock\|isSupabaseAvailable\|generateActivity" src/api/analytics.ts` -- should return zero matches.
    Run `grep -c "error:" src/api/analytics.ts` -- should show error flags in catch blocks.
    Run `npm run build` to verify no TypeScript errors.
  </verify>
  <done>
    - Zero hardcoded fake numbers (50247, 25124, 847, etc.) in any code path
    - Zero mock generator functions in the file
    - All error catch blocks return zeros + error flag string
    - All `isSupabaseAvailable` guard blocks removed
    - No fallback-to-mock patterns (ternary with mock functions)
    - `/system` endpoint returns real status only
    - File builds without errors
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "50247\|25124\|847\|generateMock\|isSupabaseAvailable" src/api/analytics.ts` returns nothing
2. `grep -rn "mock\|Mock\|FAKE\|fake" src/api/analytics.ts` returns nothing (case-sensitive check for function names)
3. `npm run build` passes
4. Every catch block in the file contains `error:` string in its response
</verification>

<success_criteria>
- ANL-01: Analytics endpoints return only real database data or explicit zeros with error flags
- ANL-02: User counts and engagement metrics are computed from actual database records
- No mock generator functions exist in the file
- No `isSupabaseAvailable` fallback pattern exists
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-performance/07-01-SUMMARY.md`
</output>
