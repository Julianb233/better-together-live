---
phase: 08-infrastructure-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.tsx
  - src/routes/pages.ts
  - src/routes/inline-api.ts
  - src/pages/home.tsx
autonomous: true

must_haves:
  truths:
    - "index.tsx is under 500 lines after extraction"
    - "All page routes still render correctly (same HTML output)"
    - "All inline API routes still function (same JSON responses)"
    - "npm run build succeeds with no new errors"
  artifacts:
    - path: "src/routes/pages.ts"
      provides: "All page route registrations extracted from index.tsx"
      exports: ["pageRoutes"]
    - path: "src/routes/inline-api.ts"
      provides: "Inline user, invite-partner, and relationship API handlers"
      exports: ["default"]
    - path: "src/pages/home.tsx"
      provides: "Homepage TSX (~1230 lines) extracted from index.tsx"
      exports: ["HomePage"]
  key_links:
    - from: "src/index.tsx"
      to: "src/routes/pages.ts"
      via: "app.route('', pageRoutes)"
      pattern: "app\\.route.*pageRoutes"
    - from: "src/index.tsx"
      to: "src/routes/inline-api.ts"
      via: "app.route('', inlineApi)"
      pattern: "app\\.route.*inlineApi"
    - from: "src/routes/pages.ts"
      to: "src/pages/home.tsx"
      via: "import { HomePage }"
      pattern: "import.*HomePage.*home"
---

<objective>
Refactor index.tsx from 1915 lines to under 500 lines by extracting inline route handlers and the homepage TSX to separate modules.

Purpose: INF-04 requires index.tsx under 500 lines with handlers extracted to API modules. This is a pure extraction refactor -- no behavior changes, no new features. It creates the clean entry point that subsequent plans (Sentry middleware, logging, health check) will build on.

Output: Three new files (src/routes/pages.ts, src/routes/inline-api.ts, src/pages/home.tsx) and a dramatically slimmed index.tsx that does only app creation, middleware, and route mounting.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-infrastructure-monitoring/08-RESEARCH.md
@src/index.tsx
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract inline API handlers and page routes from index.tsx</name>
  <files>src/routes/inline-api.ts, src/routes/pages.ts, src/pages/home.tsx</files>
  <action>
    Create `src/routes/` directory.

    **src/routes/inline-api.ts:**
    Extract these inline handlers from index.tsx (lines 129-370) into a new Hono sub-app:
    - `checkDatabase` helper function (lines 130-140)
    - POST `/api/users` (lines 143-179) -- create user
    - GET `/api/users/:userId` (lines 182-201) -- get user profile
    - PUT `/api/users/:userId` (lines 204-249) -- update user profile
    - POST `/api/invite-partner` (lines 251-316) -- invite partner
    - GET `/api/relationships/:userId` (lines 318-370) -- get relationship

    Create as `new Hono<{ Bindings: Env }>()`. Import all needed utils (generateId, getCurrentDate, getCurrentDateTime, isValidEmail, getUserByEmail, getUserById, getRelationshipByUserId, getPartnerId, etc.), createDatabase from '../db', checkOwnership/forbiddenResponse from '../lib/security', and type Env from '../types'. Export default.

    **src/routes/pages.ts:**
    Extract ALL page route registrations from index.tsx into a Hono sub-app:
    - Static HTML page routes (lines 372-438): /ai-coach.html, /mobile-ui.html, /smart-scheduling.html, /intelligent-suggestions.html, /iphone-examples.html, /member-rewards.html, /become-sponsor.html, /login.html, /dashboard.html, /admin/analytics (keep requireAuth + requireAdmin inline), /subscription-boxes.html, /in-app-purchases.html, /intimacy-challenges.html, /premium-pricing.html
    - Clean URL page routes (lines 528-620): /login, /auth/reset-password, /portal, /paywall, /notification-settings, /love-languages, /password-reset, /partner-invite, /relationship-status, /checkins, /goals, /activities, /important-dates, /challenges, /gift-subscription, /subscription, /email-preferences, /bundles, /shop
    - Homepage route (line 625): GET / renders HomePage
    - API info route (line 1856): GET /api returns JSON

    Import all page HTML modules from '../pages/*'. Import requireAuth, requireAdmin from '../lib/supabase/middleware'. Import { HomePage } from '../pages/home'. Export as `pageRoutes`.

    **src/pages/home.tsx:**
    Extract the entire homepage JSX from index.tsx line 627 through line 1854 (the closing div) into a component function. Export as `export const HomePage = () => (...)`. The JSX must be moved verbatim -- do NOT restructure, rename classes, or modify any content. This is a pure cut-and-paste extraction.
  </action>
  <verify>
    Run `ls src/routes/inline-api.ts src/routes/pages.ts src/pages/home.tsx` to confirm files exist.
    Run `wc -l src/routes/inline-api.ts src/routes/pages.ts src/pages/home.tsx` to confirm reasonable line counts (~250, ~150, ~1230 respectively).
  </verify>
  <done>Three new files exist containing all extracted code with correct imports and exports.</done>
</task>

<task type="auto">
  <name>Task 2: Slim down index.tsx to pure composition</name>
  <files>src/index.tsx</files>
  <action>
    Rewrite index.tsx to be a pure composition file. It should ONLY contain:

    1. Imports: Hono, cors, except, middleware (requireAuth, requireAdmin, supabaseCookieRelay, clearLegacyCookies), rateLimitMiddleware, renderer, type Env
    2. Import route modules: all 28 existing API route modules (analyticsApi, emailApi, etc.), plus the two new route modules (inlineApi from './routes/inline-api', { pageRoutes } from './routes/pages')
    3. App creation: `const app = new Hono<{ Bindings: Env }>()`
    4. Middleware registration: rate limiting, CORS (keep existing config verbatim), clearLegacyCookies, supabaseCookieRelay, global auth with except(), renderer
    5. Route mounting: mount inlineApi, all 28 API route modules, pageRoutes
    6. Export default app

    Remove ALL inline handler code (lines 129-370), ALL page route registrations (lines 372-438, 528-620), the homepage JSX (lines 625-1855), and the /api info route (line 1856).

    Keep the CORS configuration exactly as-is (the origin function with allowed list, env checks, CORS_ORIGINS split).

    The resulting file should be ~200-350 lines (mostly imports + route mounting).

    IMPORTANT: Do NOT remove any existing page imports that are used in pages.ts -- those imports now live in pages.ts instead. Remove unused imports from index.tsx (page HTML modules, utility functions only used by inline handlers like generateId, getCurrentDate, etc.).
  </action>
  <verify>
    Run `wc -l src/index.tsx` -- must be under 500 lines.
    Run `npx tsc --noEmit 2>&1 | head -20` -- check for new type errors (some pre-existing errors are expected, but no NEW ones from the refactor).
    Run `npm run build` -- must succeed.
  </verify>
  <done>index.tsx is under 500 lines, contains only app creation + middleware + route mounting, and the project builds successfully.</done>
</task>

</tasks>

<verification>
- `wc -l src/index.tsx` shows under 500 lines
- `npm run build` succeeds
- `grep -c "app\.\(get\|post\|put\|delete\)" src/index.tsx` shows 0 (no inline handlers remain)
- All route mounts present: `grep "app.route" src/index.tsx | wc -l` shows ~30 (28 API + 2 new)
</verification>

<success_criteria>
- INF-04 satisfied: index.tsx under 500 lines with inline handlers extracted
- All existing routes still mounted (no routes lost in refactor)
- Build passes with no new errors
- Pure extraction: zero behavior changes
</success_criteria>

<output>
After completion, create `.planning/phases/08-infrastructure-monitoring/08-01-SUMMARY.md`
</output>
