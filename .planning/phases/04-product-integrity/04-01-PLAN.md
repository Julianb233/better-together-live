---
phase: 04-product-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.tsx
autonomous: true

must_haves:
  truths:
    - "Every CTA button on the homepage navigates to a real destination (no buttons that do nothing)"
    - "No fake loading spinner appears when clicking any button"
    - "'Watch Demo' buttons are removed (no demo video exists)"
    - "Footer and social links either point to real URLs or are removed"
  artifacts:
    - path: "src/index.tsx"
      provides: "Homepage with all CTAs fixed"
      contains: "href=\"/paywall\""
  key_links:
    - from: "CTA buttons"
      to: "/paywall or /login"
      via: "anchor href"
      pattern: "<a href=\"/(paywall|login)"
---

<objective>
Fix all homepage CTA buttons so every clickable element navigates to a real destination.

Purpose: Users currently click buttons that show a fake 2-second spinner then do nothing. This is deceptive and broken. Every button must either navigate somewhere real or be removed.
Output: Updated `src/index.tsx` with all CTAs linked to real pages and fake spinner script deleted.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-product-integrity/04-RESEARCH.md
@src/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert all fake CTA buttons to real navigation links</name>
  <files>src/index.tsx</files>
  <action>
    Open src/index.tsx and make these specific changes:

    1. **Desktop nav "Get Premium Access" button (~line 589-594)**: Convert the `<button>` to `<a href="/paywall">` styled as a button. Keep existing Tailwind classes, add `inline-flex items-center justify-center` for layout.

    2. **Mobile nav "Get Premium Access" button (~line 627-632)**: Same conversion to `<a href="/paywall">`.

    3. **Hero "Get Premium Access Now" button (~line 683-689)**: Convert to `<a href="/paywall">`. Remove the `<span className="relative z-10">` wrapper (unnecessary for a link). Keep the crown icon.

    4. **Hero "Watch 2-Min Demo" button (~line 691-693)**: DELETE this button entirely. No demo video exists. Do not replace with anything.

    5. **Pricing section "Transform Our Relationship" button (~line 1455-1457)**: Convert to `<a href="/paywall">`.

    6. **Pricing section "Try It Out First" button (~line 1506-1508)**: Convert to `<a href="/paywall">`.

    7. **Final CTA "Get Premium Access" button (~line 1533-1536)**: Convert to `<a href="/paywall">`.

    8. **Final CTA "Watch How It Works" button (~line 1537-1540)**: DELETE this button entirely. No demo exists.

    Pattern for each conversion:
    - Change `<button className="...">` to `<a href="/paywall" className="... inline-flex items-center justify-center">`
    - Change closing `</button>` to `</a>`
    - Remove any `<span className="relative z-10">` wrappers inside (not needed for links)

    9. **Nav "Premium" link (~line 641)**: Change `href="/premium-pricing.html"` to `href="/paywall"`.

    10. **Mobile nav "Premium" link (~line 676)**: Change `href="/premium-pricing.html"` to `href="/paywall"`.

    11. **Footer "Premium" link (~line 1573)**: Change `href="/premium-pricing.html"` to `href="/paywall"`.
  </action>
  <verify>
    Run: `grep -n 'premium-pricing.html' src/index.tsx` -- should return NO results.
    Run: `grep -c 'href="/paywall"' src/index.tsx` -- should return 8+ results (nav links + CTA buttons).
    Run: `grep -n 'Watch.*Demo\|Watch How' src/index.tsx` -- should return NO results.
    Run: `npm run build` -- should succeed without errors.
  </verify>
  <done>All CTA buttons on homepage are `<a>` tags linking to `/paywall` or `/login`. No "Watch Demo" buttons exist. All "Premium" nav links point to `/paywall`.</done>
</task>

<task type="auto">
  <name>Task 2: Delete fake loading spinner script and fix footer links</name>
  <files>src/index.tsx</files>
  <action>
    1. **Delete the fake CTA loading spinner** (~lines 1753-1767): Find and delete the entire block that starts with `// Add loading states to CTA buttons` and includes the `ctaButtons.forEach` with the `setTimeout` that shows a spinner for 2 seconds. Delete from `const ctaButtons = document.querySelectorAll` through the closing `});` of the forEach.

    2. **Fix social media links in footer** (~lines 1558-1567): These links go to "#". Change them:
       - If real social media URLs exist for Better Together, use those
       - If not, REMOVE the social media icon links entirely (do not leave `href="#"` links)
       - Keep the footer structure, just remove the dead social links

    3. **Fix footer navigation links** (~lines 1576-1587): Links like "About", "Careers", "Blog", "Press" that go to "#":
       - Remove links that go to "#" -- these pages don't exist
       - Keep links that go to real routes (e.g., `/privacy`, `/terms`, `/paywall`)
       - For "About" -- if no `/about` page exists, remove the link

    4. **Verify no other buttons remain without navigation**: Search for any remaining `<button` tags in the homepage JSX that lack an `onclick` handler or are not inside a `<form>`. If found, convert to `<a>` tags pointing to appropriate pages or remove them.
  </action>
  <verify>
    Run: `grep -n 'ctaButtons' src/index.tsx` -- should return NO results.
    Run: `grep -n 'setTimeout.*2000' src/index.tsx` -- should return NO results related to button spinners.
    Run: `grep -c 'href="#"' src/index.tsx` -- should return 0 or very few results (only legitimate anchor links).
    Run: `npm run build` -- should succeed.
  </verify>
  <done>Fake 2-second spinner script is deleted. Footer social links and navigation links either point to real pages or are removed. No `href="#"` dead links remain in footer.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. `grep -c 'href="#"' src/index.tsx` returns 0 (no dead links)
3. `grep 'premium-pricing.html' src/index.tsx` returns nothing
4. `grep 'ctaButtons' src/index.tsx` returns nothing
5. `grep 'Watch.*Demo' src/index.tsx` returns nothing
6. All `<button>` tags in homepage JSX either have form context or are removed
</verification>

<success_criteria>
Every CTA button on the homepage navigates to a real destination. No fake loading spinners. No dead "#" links in footer. No references to premium-pricing.html. Build passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-integrity/04-01-SUMMARY.md`
</output>
