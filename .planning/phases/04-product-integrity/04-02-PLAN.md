---
phase: 04-product-integrity
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/index.tsx
  - src/pages/login.ts
  - src/pages/intimacy-challenges.ts
  - src/pages/become-sponsor.ts
  - src/pages/subscription-boxes.ts
  - src/pages/paywall.ts
  - src/pages/smart-scheduling.ts
  - src/pages/intelligent-suggestions.ts
  - src/pages/in-app-purchases.ts
  - src/pages/premium-pricing.ts
autonomous: true

must_haves:
  truths:
    - "No hardcoded user counts (50,000, 50k, 500+) appear on any page"
    - "No fabricated statistics (87%, 94%, 98%) appear on any page"
    - "No 'end-to-end encryption' claims exist (app uses TLS only)"
    - "No internal business metrics (ARPU, LTV, conversion rates) are publicly visible"
    - "/in-app-purchases.html route returns 404"
    - "/premium-pricing.html redirects to /paywall"
  artifacts:
    - path: "src/index.tsx"
      provides: "Homepage without fake social proof"
    - path: "src/pages/login.ts"
      provides: "Login page with accurate security claims"
    - path: "src/pages/intimacy-challenges.ts"
      provides: "Intimacy page with accurate encryption claims"
  key_links:
    - from: "/premium-pricing.html route"
      to: "/paywall"
      via: "redirect"
      pattern: "redirect.*paywall"
---

<objective>
Remove all fake social proof, fabricated statistics, false encryption claims, and exposed business metrics from every page.

Purpose: The app currently makes false claims (50,000+ users, 87% improvement, end-to-end encryption) that don't exist. This undermines trust and creates legal risk. Every claim must be either verifiably true or removed.
Output: All pages cleaned of fabricated numbers and false claims. In-app-purchases page deleted, premium-pricing redirected.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-product-integrity/04-RESEARCH.md
@.planning/phases/04-product-integrity/04-01-SUMMARY.md
@src/index.tsx
@src/pages/login.ts
@src/pages/intimacy-challenges.ts
@src/pages/become-sponsor.ts
@src/pages/subscription-boxes.ts
@src/pages/paywall.ts
@src/pages/in-app-purchases.ts
@src/pages/premium-pricing.ts
@src/pages/smart-scheduling.ts
@src/pages/intelligent-suggestions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove fake social proof and false claims from homepage and paywall</name>
  <files>src/index.tsx, src/pages/paywall.ts</files>
  <action>
    **In src/index.tsx:**

    1. **Hero badge (~line 660)**: Replace "Trusted by 50,000+ Couples" with "AI-Powered Relationship Platform for Couples". Remove the `animate-pulse` and `animate-ping` classes (urgency manipulation).

    2. **Avatar badge "+50k" (~line 728)**: Remove the "+50k" text entirely. Keep the avatar images if they exist, just remove the count badge.

    3. **Stats section (~lines 751-767)**: The fabricated stats block showing "87% Improved Communication", "94% Relationship Satisfaction", "15+ Activities", "98% Would Recommend" -- REMOVE the entire stats grid. Do not replace with different numbers. If you want to keep structure, replace with feature highlights (e.g., "AI Coaching", "Daily Check-ins", "Shared Goals", "Date Activities") that describe real features without making unverified claims.

    4. **"Join 50,000+ Couples" in pricing section (~line 1399)**: Change to "Join Couples Building Stronger Relationships" or simply "Choose Your Plan".

    5. **"End-to-end data encryption" (~line 1286)**: Change to "Data encrypted in transit and at rest" (this is accurate -- Supabase and Vercel both use TLS).

    **In src/pages/paywall.ts:**

    6. **"Join 50,000+ Happy Couples" heading**: Change to "Start Your Relationship Journey" or "Choose Your Plan".

    7. **Fabricated stats grid (87%, 94%, 15+, 98%)**: Remove the entire stats section. Same approach as homepage.

    8. **Fake testimonial "Sarah & Mike, married 8 years"**: Remove the testimonial block entirely. Do not replace with a different fake testimonial.

    9. **"Limited Time Offer" urgency block**: Remove. There is no actual time-limited offer.
  </action>
  <verify>
    Run: `grep -rn '50,000\|50k\|50K' src/index.tsx src/pages/paywall.ts` -- should return NO results.
    Run: `grep -rn '87%\|94%\|98%' src/index.tsx src/pages/paywall.ts` -- should return NO results related to fake stats.
    Run: `grep -rn 'end-to-end' src/index.tsx` -- should return NO results (replaced with accurate claim).
    Run: `grep -rn 'Limited Time' src/pages/paywall.ts` -- should return NO results.
    Run: `npm run build` -- should succeed.
  </verify>
  <done>Homepage and paywall have zero fabricated numbers, zero fake testimonials, zero false urgency. All remaining claims are verifiably true.</done>
</task>

<task type="auto">
  <name>Task 2: Fix false claims in secondary pages, delete/redirect problem pages</name>
  <files>src/pages/login.ts, src/pages/intimacy-challenges.ts, src/pages/become-sponsor.ts, src/pages/subscription-boxes.ts, src/pages/smart-scheduling.ts, src/pages/intelligent-suggestions.ts, src/pages/in-app-purchases.ts, src/pages/premium-pricing.ts, src/index.tsx</files>
  <action>
    **Fix false encryption claims:**

    1. **src/pages/login.ts (~line 347)**: Change "Enterprise-grade security with end-to-end encryption" to "Secured with bank-level TLS encryption".

    2. **src/pages/intimacy-challenges.ts (~line 158)**: Change "End-to-end encryption" badge to "Data encrypted in transit and at rest".

    3. **src/pages/intimacy-challenges.ts (~lines 549-550)**: Change "End-to-End Encryption" section title and "military-grade security" claim to "TLS Encryption" and "Bank-level TLS security for all data".

    **Fix fake numbers in secondary pages:**

    4. **src/pages/become-sponsor.ts**: Remove "50,000+ engaged couples" and "500+ premium brands" from meta description and body. Replace with honest language like "a growing community of couples" and "brand partnership opportunities". Also remove "$2.4K+ annually" spending claim.

    5. **src/pages/subscription-boxes.ts**: Remove "60-70% margins" from meta description (internal business metric). Remove any "50,000" references. Any buttons that only `console.log` -- convert to `<a href="/paywall">` or remove.

    6. **src/pages/smart-scheduling.ts**: Search for and remove any "50,000" or fabricated stat references.

    7. **src/pages/intelligent-suggestions.ts**: Search for and remove any "50,000" or fabricated stat references.

    **Delete and redirect problem pages:**

    8. **Delete in-app-purchases route from src/index.tsx**: Find the route handler for `/in-app-purchases.html` (~line 422-424) and DELETE it entirely. Do NOT delete the file `src/pages/in-app-purchases.ts` yet (it won't be served without the route, and can be cleaned up later). The page exposes ARPU, LTV, conversion targets -- it must not be publicly accessible.

    9. **Redirect premium-pricing to paywall in src/index.tsx**: Replace the `/premium-pricing.html` route handler (~line 434-436) with a redirect:
    ```typescript
    app.get('/premium-pricing.html', (c) => {
      return c.redirect('/paywall', 301);
    })
    ```
    This ensures any existing links/bookmarks still work but land on the canonical pricing page.

    **Comprehensive grep verification:**
    After all edits, run a broad search across ALL files in src/ for remaining instances of: "50,000", "50k", "500+", "end-to-end encrypt", "military-grade", "ARPU", "LTV", "conversion rate". Fix any remaining instances found.
  </action>
  <verify>
    Run: `grep -rn '50,000\|50k\|50K' src/` -- should return NO results (or only in comments explaining removal).
    Run: `grep -rn 'end-to-end' src/` -- should return NO results.
    Run: `grep -rn 'military-grade' src/` -- should return NO results.
    Run: `grep -rn '500+ \(premium\|brands\)' src/` -- should return NO results.
    Run: `grep -rn 'ARPU\|LTV.*420\|conversion rate.*12' src/pages/in-app-purchases.ts` -- file may exist but route is gone.
    Run: `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/in-app-purchases.html` during dev -- should return 404.
    Run: `npm run build` -- should succeed.
  </verify>
  <done>No false encryption claims anywhere in codebase. No fabricated user counts or statistics on any page. In-app-purchases route deleted (returns 404). Premium-pricing redirects to /paywall. All remaining content is honest and verifiable.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `grep -rn '50,000\|50k' src/` returns zero results
3. `grep -rn 'end-to-end' src/` returns zero results
4. `grep -rn 'military-grade' src/` returns zero results
5. The route for `/in-app-purchases.html` no longer exists in index.tsx
6. The route for `/premium-pricing.html` redirects to `/paywall`
7. No fabricated percentages (87%, 94%, 98%) remain on any public page
</verification>

<success_criteria>
Zero fake social proof numbers across the entire codebase. Zero false encryption claims. Internal business metrics page inaccessible. Premium pricing redirects to canonical paywall. All claims on all pages are verifiably true.
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-integrity/04-02-SUMMARY.md`
</output>
