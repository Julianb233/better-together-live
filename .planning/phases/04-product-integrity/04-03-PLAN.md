---
phase: 04-product-integrity
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/index.tsx
  - src/pages/paywall.ts
  - src/pages/subscription-boxes.ts
  - src/pages/member-rewards.ts
autonomous: true

must_haves:
  truths:
    - "Homepage pricing section matches paywall pricing (Growing Together $39/mo, Growing Together+ $69/mo)"
    - "Only one pricing page exists with Stripe checkout (/paywall)"
    - "Unimplemented features (subscription boxes, human coaching, rewards, surprise credits) are marked 'Coming Soon' or removed from navigation"
    - "No conflicting price points appear across any pages"
  artifacts:
    - path: "src/index.tsx"
      provides: "Homepage with unified pricing matching paywall"
    - path: "src/pages/paywall.ts"
      provides: "Canonical pricing page (source of truth)"
  key_links:
    - from: "Homepage pricing section"
      to: "/paywall"
      via: "matching prices and CTA links"
      pattern: "\\$39|\\$69|href=\"/paywall\""
---

<objective>
Unify pricing across all pages to match the canonical paywall (Growing Together $39/mo, Growing Together+ $69/mo) and mark unimplemented features as "Coming Soon".

Purpose: Three conflicting pricing models confuse users and create legal risk. The paywall has the actual Stripe checkout integration, so it is the source of truth. All other pricing references must match or link to it. Unimplemented features (subscription boxes, human coaching, rewards) must not be presented as available.
Output: Consistent pricing across homepage and paywall. Unimplemented features clearly marked.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-product-integrity/04-RESEARCH.md
@.planning/phases/04-product-integrity/04-02-SUMMARY.md
@src/index.tsx
@src/pages/paywall.ts
@src/pages/subscription-boxes.ts
@src/pages/member-rewards.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify homepage pricing to match paywall canonical pricing</name>
  <files>src/index.tsx</files>
  <action>
    The canonical pricing is in `src/pages/paywall.ts`:
    - **Growing Together**: $39/month (for couple)
    - **Growing Together+**: $69/month (for couple), includes 7-day free trial

    In src/index.tsx, the homepage currently shows "$240/year" and "$30/month" pricing (matching the now-redirected premium-pricing page). Update ALL pricing references:

    1. **Pricing card 1 (~lines 1418-1425)**: Currently shows "Better Together Plan $240/year" ($20/mo). Change to:
       - Plan name: "Growing Together"
       - Price: "$39/month"
       - Subtext: "per couple" or "billed monthly"
       - Keep the feature list but ensure it matches what the paywall describes for this tier
       - The CTA button (already converted to `<a href="/paywall">` in Plan 01) stays as-is

    2. **Pricing reference near line 698-702**: If there's a "$240/year" reference in the hero area, update to "$39/mo" or remove the price mention from the hero.

    3. **Pricing card 2 (~lines 1471-1474)**: Currently shows "Try It Out $30/month". Change to:
       - Plan name: "Growing Together+"
       - Price: "$69/month"
       - Subtext: "per couple - 7-day free trial"
       - Update feature list to match paywall's premium tier
       - CTA button already links to `/paywall`

    4. **Any other price mentions**: Search for `$240`, `$30/mo`, `$20/mo`, `$289` in index.tsx and update or remove. These are all from the old conflicting pricing models.

    5. **Pricing section header**: If it says anything about "simple pricing" or "affordable plans", keep it. Just ensure the dollar amounts and plan names match paywall exactly.
  </action>
  <verify>
    Run: `grep -n '\$240\|\$289\|\$30/mo\|\$20/mo' src/index.tsx` -- should return NO results.
    Run: `grep -n '\$39\|\$69' src/index.tsx` -- should return results matching the canonical pricing.
    Run: `grep -n 'Growing Together' src/index.tsx` -- should return the plan names.
    Run: `npm run build` -- should succeed.
  </verify>
  <done>Homepage pricing section shows Growing Together $39/mo and Growing Together+ $69/mo, exactly matching the paywall. No conflicting prices exist anywhere on the homepage.</done>
</task>

<task type="auto">
  <name>Task 2: Mark unimplemented features as "Coming Soon" and audit remaining pages</name>
  <files>src/index.tsx, src/pages/subscription-boxes.ts, src/pages/member-rewards.ts</files>
  <action>
    **Identify unimplemented features** (per INT-04): subscription boxes, human coaching, member rewards/savings, surprise credits. These features have pages but no backend implementation.

    1. **src/pages/subscription-boxes.ts**: Add a prominent "Coming Soon" banner at the top of the page content. Something like:
       ```html
       <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-6 text-center">
         <span class="text-amber-800 font-semibold">Coming Soon</span>
         <p class="text-amber-700 text-sm mt-1">Subscription boxes are currently in development.</p>
       </div>
       ```
       Also ensure any "Subscribe Now" or "Order" buttons are disabled or replaced with "Notify Me" (linking to nothing, or removed). Remove any pricing that conflicts with the paywall model. Remove any buttons that `console.log` only.

    2. **src/pages/member-rewards.ts**: Add the same "Coming Soon" banner. Disable or remove any "Redeem" or purchase buttons. These rewards don't exist yet.

    3. **Homepage nav/sections referencing unimplemented features**: In src/index.tsx, check if navigation or body sections link to subscription-boxes, member-rewards, or human coaching pages. For any such links:
       - If in main navigation: keep the link but add a "(Coming Soon)" text label after the link text
       - If in a feature card: add a "Coming Soon" badge to the card
       - If it's a standalone CTA for an unimplemented feature: remove it

    4. **Final consistency audit**: Do a quick scan of ALL pricing references across the entire `src/pages/` directory:
       ```
       grep -rn '\$[0-9]' src/pages/
       ```
       Any price that conflicts with the canonical $39/$69 model should be updated to match OR removed if it's for an unimplemented feature. The only valid prices are:
       - Growing Together: $39/month
       - Growing Together+: $69/month
       - Any subscription box / reward pricing should say "Pricing TBD" or be removed

    5. **Ensure paywall.ts is clean**: After Plans 01 and 02 cleaned up fake stats and testimonials, verify paywall.ts still renders correctly. Its pricing ($39/$69) should remain untouched. Its Stripe checkout integration should remain untouched.
  </action>
  <verify>
    Run: `grep -rn 'Coming Soon\|coming soon\|coming-soon' src/pages/subscription-boxes.ts src/pages/member-rewards.ts` -- should return results in both files.
    Run: `grep -rn '\$240\|\$289\|\$30/mo\|\$20/mo\|\$19/mo\|\$79/mo' src/` -- should return NO results (all old pricing removed).
    Run: `npm run build` -- should succeed.
    Manually verify: The paywall page at `/paywall` still shows $39 and $69 pricing with working Stripe checkout buttons.
  </verify>
  <done>Single consistent pricing model ($39/$69) across all pages. Unimplemented features (subscription boxes, member rewards) clearly marked "Coming Soon". No conflicting prices anywhere in the codebase. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `grep -rn '\$240\|\$289\|\$30/mo\|\$20/mo' src/` returns zero results
3. Homepage pricing shows $39/mo and $69/mo matching paywall
4. Subscription boxes page has "Coming Soon" banner
5. Member rewards page has "Coming Soon" banner
6. No conflicting pricing exists across any page
7. Paywall Stripe checkout integration untouched and functional
</verification>

<success_criteria>
Single pricing model (Growing Together $39/mo, Growing Together+ $69/mo) displayed consistently across homepage, paywall, and any page that mentions pricing. Unimplemented features clearly marked as "Coming Soon". Build passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-integrity/04-03-SUMMARY.md`
</output>
