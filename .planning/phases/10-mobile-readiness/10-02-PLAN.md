---
phase: 10-mobile-readiness
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - mobile/src/screens/LoginScreen.tsx
  - mobile/src/screens/RegisterScreen.tsx
  - mobile/src/utils/websocket.ts
autonomous: true

must_haves:
  truths:
    - "LoginScreen has email and password fields (no name field) and calls login(email, password)"
    - "RegisterScreen has email, password, and name fields and calls register(email, password, name)"
    - "RegisterScreen does NOT call navigation.replace('MainTabs') -- auth state change drives navigation automatically"
    - "WebSocket authenticates with JWT token instead of raw userId query parameter"
    - "Mobile app builds without TypeScript errors"
  artifacts:
    - path: "mobile/src/screens/LoginScreen.tsx"
      provides: "Login form with email + password fields using Supabase auth"
      contains: "password"
    - path: "mobile/src/screens/RegisterScreen.tsx"
      provides: "Registration form with email + password + name, handles confirmationRequired"
      contains: "confirmationRequired"
    - path: "mobile/src/utils/websocket.ts"
      provides: "WebSocket client using JWT token for authentication"
      contains: "Authorization"
  key_links:
    - from: "mobile/src/screens/LoginScreen.tsx"
      to: "mobile/src/hooks/useAuth.ts"
      via: "login(email, password) call"
      pattern: "login\\(email.*password\\)"
    - from: "mobile/src/screens/RegisterScreen.tsx"
      to: "mobile/src/hooks/useAuth.ts"
      via: "register(email, password, name) call"
      pattern: "register\\(email.*password.*name\\)"
    - from: "mobile/src/utils/websocket.ts"
      to: "mobile/src/lib/supabase.ts"
      via: "JWT token from Supabase session"
      pattern: "supabase.*getSession|access_token"
---

<objective>
Update mobile auth screens (Login, Register) to use password-based authentication, fix WebSocket auth to use JWT tokens, and verify the app builds cleanly.

Purpose: MOB-01, MOB-02, MOB-03 -- Screens must match the new Supabase Auth flow from Plan 01. WebSocket must stop sending raw userId. App must compile.
Output: Working auth screens with password fields, JWT-authenticated WebSocket, clean build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-mobile-readiness/10-RESEARCH.md
@.planning/phases/10-mobile-readiness/10-01-SUMMARY.md

Key source files:
@mobile/src/screens/LoginScreen.tsx
@mobile/src/screens/RegisterScreen.tsx
@mobile/src/utils/websocket.ts
@mobile/src/navigation/AppNavigator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LoginScreen and RegisterScreen for password-based auth</name>
  <files>mobile/src/screens/LoginScreen.tsx, mobile/src/screens/RegisterScreen.tsx</files>
  <action>
1. Update `mobile/src/screens/LoginScreen.tsx`:
   - Replace the `name` state variable with `password` state variable
   - Replace the Name `<Input>` field with a Password `<Input>` field: `label="Password"`, `placeholder="Enter your password"`, `secureTextEntry={true}`, `autoComplete="password"`
   - Update `validateForm`: remove name validation, add password validation (required, min 6 characters -- Supabase default minimum)
   - Update `errors` type: replace `name?: string` with `password?: string`
   - Update `handleLogin`: call `login(email.trim(), password)` instead of `login(email.trim(), name.trim())`
   - No navigation call needed after login -- AppNavigator already switches stacks based on `isAuthenticated` from `onAuthStateChange`
   - Add "Forgot Password?" text/button below the password field that shows an Alert with message "Password reset is available on the web app at better-together.live" (full mobile forgot-password flow is v2 scope)

2. Update `mobile/src/screens/RegisterScreen.tsx`:
   - Add `password` state variable (keep `name` and `email`, remove `phone` -- simplify registration)
   - Add Password `<Input>` field between Email and the button: `label="Password"`, `placeholder="Min 6 characters"`, `secureTextEntry={true}`, `autoComplete="new-password"`
   - Update `validateForm`: add password validation (required, min 6 chars), remove phone validation
   - Update `errors` type: add `password?: string`, remove `phone?: string`
   - Import `register` from `useAuth` instead of using `login` for registration
   - Update `handleRegister`: call `register(email.trim(), password, name.trim())` instead of `login(email.trim(), name.trim())`
   - Handle `confirmationRequired` response: show Alert "Check Your Email - Please confirm your email address before logging in." and do NOT navigate
   - REMOVE the `navigation.replace('MainTabs')` call entirely -- auth state drives navigation automatically via AppNavigator's `isAuthenticated` check
   - On success without confirmation required: do nothing (onAuthStateChange will fire and AppNavigator will switch to MainTabs automatically)
  </action>
  <verify>
  - `grep "password" mobile/src/screens/LoginScreen.tsx` finds password state and input
  - `grep "secureTextEntry" mobile/src/screens/LoginScreen.tsx` confirms password field is masked
  - `grep "secureTextEntry" mobile/src/screens/RegisterScreen.tsx` confirms password field is masked
  - `grep "navigation.replace" mobile/src/screens/RegisterScreen.tsx` returns NO results
  - `grep "confirmationRequired" mobile/src/screens/RegisterScreen.tsx` confirms email confirmation handling
  - `grep "register(" mobile/src/screens/RegisterScreen.tsx` confirms register function usage (not login)
  </verify>
  <done>LoginScreen has email+password fields calling login(email, password). RegisterScreen has name+email+password fields calling register(email, password, name) with email confirmation handling. No manual navigation -- auth state drives it.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WebSocket auth and verify build</name>
  <files>mobile/src/utils/websocket.ts</files>
  <action>
1. Update `mobile/src/utils/websocket.ts`:
   - Import `supabase` from `../lib/supabase`
   - In the `connect()` method, replace the AsyncStorage userId lookup with Supabase session token:
     ```
     const { data: { session } } = await supabase.auth.getSession()
     const token = session?.access_token
     const wsUrl = `${this.config.url}${token ? `?token=${token}` : ''}`
     ```
   - Remove the AsyncStorage import (no longer needed in this file)
   - Remove the `@better_together:user_id` storage key reference
   - The WebSocket server will need to verify this token server-side (that's a backend concern, not mobile scope -- but sending the token is the mobile responsibility)

2. Run build verification:
   - Run `cd mobile && npx tsc --noEmit` to check for TypeScript errors
   - If there are type errors, fix them. Common issues:
     - `useAuth` return type may need updating if screens expect old `login(email, name)` signature
     - `apiClient.createUser` removal may cause errors in files that import it (check RegisterScreen)
     - `Session` import from `@supabase/supabase-js` may need type-only import
   - If `node_modules` doesn't exist, run `npm install` first
   - Fix any remaining TypeScript errors until `tsc --noEmit` passes cleanly

NOTE on build: If `npm install` fails or `tsc` reveals many pre-existing errors unrelated to our changes, document them in the summary but don't chase unrelated issues. Focus on ensuring OUR changes compile correctly.
  </action>
  <verify>
  - `grep "AsyncStorage" mobile/src/utils/websocket.ts` returns NO results
  - `grep "user_id" mobile/src/utils/websocket.ts` returns NO results (no raw userId)
  - `grep "access_token\|token" mobile/src/utils/websocket.ts` confirms JWT token usage
  - `cd mobile && npx tsc --noEmit` exits with code 0 (or document pre-existing errors)
  </verify>
  <done>WebSocket sends JWT token instead of raw userId. TypeScript build passes (or pre-existing errors documented). All auth-related files compile cleanly against each other.</done>
</task>

</tasks>

<verification>
1. `grep -r "X-User-ID" mobile/src/` -- must return nothing (from Plan 01 + this plan combined)
2. `grep -r "navigation.replace" mobile/src/screens/RegisterScreen.tsx` -- must return nothing
3. `grep "secureTextEntry" mobile/src/screens/LoginScreen.tsx mobile/src/screens/RegisterScreen.tsx` -- both screens have password fields
4. `grep "supabase" mobile/src/utils/websocket.ts` -- WebSocket uses Supabase
5. `cd mobile && npx tsc --noEmit` -- clean build
</verification>

<success_criteria>
- LoginScreen: email + password fields, calls login(email, password), no name field
- RegisterScreen: name + email + password fields, calls register(email, password, name), handles confirmationRequired, no navigation.replace
- WebSocket: sends JWT token from Supabase session, no raw userId
- TypeScript: `tsc --noEmit` passes (or only pre-existing errors remain)
- No references to X-User-ID, createUser auth pattern, or manual userId storage remain
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-readiness/10-02-SUMMARY.md`
</output>
